#include <bits/stdc++.h>
#define ll long long
using namespace std;
int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int t;
    cin >> t;
    while (t--) {
        ll n,m,x,y;
        cin >> n >> m >> x >> y;
        for (ll i=0;i<n;i++){
            ll a; 
            cin>>a;
        }
        for (ll i=0;i<m;i++){
            ll b; 
            cin>>b;
        }
        cout << (n + m) << endl;
    }
    return 0;
}
